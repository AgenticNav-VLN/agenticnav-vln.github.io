# AgenticNav 项目网站进展

## 2026-09-23 网站重做（进行中）

- 用户确认：只收录最新五个二维实机演示（长程、Building 22、厨房冰箱、垃圾桶、乒乓球桌），旧版二维及所有三维演示均不加入。
- 五片完整过程恒定10倍速，无速度选择；指令独立显示并支持点击短语跳转、播放同步高亮；网页版另存无画内指令视频。
- 封面采用长程实机穿门、大厅、室外与道路选段，约25秒静音循环。
- 全站Calibri加粗斜体；深蓝视频封面、白色内容区、蓝色交互强调。方法与引言拆成网页元素，支持分步演示。
- 公开与匿名站研究内容一致，匿名站保留现有入口并移除身份信息；本轮不修改论文源稿或原视频。
- 已核实实际公开地址为 https://agenticnav-vln.github.io/，旧记录中的 agenticnav.github.io 为过期地址。
- 已完成页面、约25秒封面与五个720p30静音10倍速视频；长程74秒、Building22约37.267秒、厨房约34.267秒、垃圾桶约7.333秒、球桌约11.367秒。
- 用户确认复用已打码素材，不对旁人额外打码。厨房、Building22及回看图片已改用既有匿名版本；双站视频相同，原素材和原片不变。
- 浏览器已核验指令跳转、同步高亮、记忆弹窗、深度交互；390像素手机布局无横向溢出。播放器兼容不支持分段下载的匿名代理，必要时仅在用户跳转后加载同源视频的本地副本。
- HTML引用、身份信息、媒体元数据、原速至10倍速时间换算、双站一致性和六片完整解码通过。复现脚本在scripts，详细报告在外层local_data/website-redesign。
- 待完成：提交推送、匿名服务刷新、实际网址的视频播放核验。

## 仓库范围

- 本仓库维护 AgenticNav 的公开静态项目网站。
- 主分支为 `main`，通过 GitHub Pages 发布到 `https://agenticnav-vln.github.io/`。
- 网站源码不包含导航运行代码、模型密钥、实验日志或原始机器人数据。
- 本仓库作为二维/仿真外层工作区 `sim` 分支的子模块登记。

## 当前状态

- 项目主页、论文链接、方法介绍、工具示例图片和项目视频已经发布。
- 页面不依赖构建框架，入口为 `index.html`，样式和脚本位于 `static/`。
- 2026-09-07 补齐本进展文件，并完成本地、GitHub 与外层子模块版本核对。

## 后续修改要求

- 更新论文、视频或图片后，同时检查页面链接和移动端显示。
- 大型原始素材继续保存在统一实验与论文目录，不重复放入网站仓库。
- 每次修改后先检查工作区状态，再提交、推送并更新外层子模块版本。
- 远端匿名兼容：匿名服务不支持Range且其隔离策略阻止fetch，新增按需*.media.js传递同一视频字节，再生成可跳转的浏览器媒体副本。普通公开站继续直接使用MP4；本地已按相同隔离头复现问题。

- 实际匿名服务对较大文本传输返回502；媒体脚本改为每块1.5 MiB原视频字节、最多三块并发，提供百分比、取消和超时。分块重组与原MP4逐字节一致。
