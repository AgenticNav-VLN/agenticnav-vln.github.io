{
  "name": "agenticnav-project-page",
  "private": true,
  "scripts": {
    "build:scene": "esbuild src/tool-scene.js --bundle --minify --format=iife --target=es2020 --outfile=static/js/tool-scene.js --legal-comments=eof",
    "test:scene": "node --test scripts/scene.test.mjs"
  },
  "dependencies": { "three": "0.186.0" },
  "devDependencies": { "esbuild": "0.28.2" }
}
